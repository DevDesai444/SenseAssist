# SenseAssist

## Local Verification

Run all current verification checks in one command:

```bash
make status
```

Useful sub-commands:

```bash
make test
make helper-health
make sync-all-demo
make db-summary
```
